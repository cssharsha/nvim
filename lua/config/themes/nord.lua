return {
    {
        name = "Nord",
        colorscheme = "nord",
        -- Nord often doesn't require specific 'before' commands unless customizing heavily
        -- before = [[ vim.g.nord_contrast = true ]], -- Example customization
    },
}
